import React from "react";
import styled from "styled-components";

const StyledNoticeIcon = styled.span`
  height: 46px;
  font-size: 32px;
  display: flex;
  align-items: center;
`;

export default StyledNoticeIcon